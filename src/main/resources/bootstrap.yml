spring:
  profiles:
    active: dev

---

spring:
  profiles: dev
  application:
    name: template-service
  cloud:
    config:
      uri:
      username:
      password:

---

spring:
  profiles: prod
  application:
    name: template-service
  cloud:
    config:
      uri:
      username:
      password: